# コードレビュー結果

対象: `src/App.jsx`, `app.py`

## 総評

- UI/UX の完成度は高く、モバイル前提の TODO アプリとして主要機能（追加・編集・削除・サブタスク・検索・フィルタ・テーマ/言語切替）が一通り揃っています。
- 一方で、**カスタムカテゴリ対応時の表示崩れ**と、**ID 生成方式による衝突リスク**は優先して対処したほうがよいです。

## 指摘事項（優先度順）

### 1) カスタムカテゴリ名でラベルが欠落する可能性（High）

- 該当箇所: `src/App.jsx`
- 現状、カテゴリ表示で `t[category.toLowerCase()]` を使っています。
- `translations` に存在しないカテゴリ（将来カテゴリ追加を許容した場合）では `undefined` が表示され、UI が崩れます。

**提案**

- 表示時にフォールバックを入れる（例: `t[key] ?? category`）。

### 2) タスク/サブタスクの ID 生成が `Date.now()` 依存（Medium）

- 該当箇所: `src/App.jsx`
- `Date.now()` は短時間の連続操作で衝突する可能性があり、React key 重複や更新先誤りの温床になります。

**提案**

- `crypto.randomUUID()` を優先し、非対応環境のみフォールバックする。

### 3) カテゴリ選択の先頭 option が固定で `Work` 扱い（Low）

- 該当箇所: `src/App.jsx`
- 追加フォームで `<option value="">{t.work}</option>` となっており、見た目は「仕事」選択肢が重複します。
- 実装上は `selectedCategory || categories[0]` なので動作はしますが、UI の意図が伝わりにくいです。

**提案**

- 先頭 option をプレースホルダ（例:「カテゴリを選択」）にするか、先頭 option 自体を削除して `categories` のみ表示する。

## `app.py` について

- 構成はシンプルで読みやすいです。
- ただし `clear_all()` が `task != ''` 条件の delete になっており、空文字タスクが DB に残る設計です。

**提案**

- テーブル全削除が意図ならより明示的な条件設計にする（運用上の安全装置が必要なら UI 側で確認ダイアログを追加）。

## 参考（良い点）

- 期限日のローカル日付パース処理を分離しており、タイムゾーン罠への意識がある。
- `useMemo` でフィルタ・統計計算を整理していて、可読性が高い。
- テーマ変数を CSS カスタムプロパティに寄せているため、拡張が容易。
